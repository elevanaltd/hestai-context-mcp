"""Port (abstract contract) packages for hestai-context-mcp.

Modules in this package define *provider-agnostic* interfaces only —
see PROD::I3 PROVIDER_AGNOSTIC_CONTEXT. Concrete provider-specific
implementations live under :mod:`hestai_context_mcp.adapters`.
"""
